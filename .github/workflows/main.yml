on:
  push:
    branches:
      - main
name: Deploy to cPanel
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - name: Get Latest Code
        uses: actions/checkout@v2
      # Deploy to cPanel
      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          ftp-server: ftp.ronaldoservices.com.br
          ftp-username: ronaldo@ronaldoservices.com.br
          ftp-password: Tchuca101.
